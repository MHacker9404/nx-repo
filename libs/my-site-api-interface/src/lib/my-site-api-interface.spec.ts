import { mySiteApiInterface } from './my-site-api-interface';

describe('mySiteApiInterface', () => {
  it('should work', () => {
    expect(mySiteApiInterface()).toEqual('my-site-api-interface');
  });
});
