import { boardGameReviewApiInterface } from './board-game-review-api-interface';

describe('boardGameReviewApiInterface', () => {
  it('should work', () => {
    expect(boardGameReviewApiInterface()).toEqual(
      'board-game-review-api-interface'
    );
  });
});
